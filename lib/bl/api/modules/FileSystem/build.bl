__setup :: fn () {
    switch OS_KIND {
        OSKind.Windows { target_windows(exe); }
        OSKind.MacOS   { target_macos(exe);   }
        default        { panic("Unknown build target!"); }
    }
}