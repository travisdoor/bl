#load "libc.bl"

main : fn () -> s32 {
  A : 666;
  B : A;
  C : B;

  True : true;

  if True {
    printf("A = %d\n", A);
    printf("B = %d\n", B);
    printf("C = %d\n", C);
  } else {
    puts("INVALID");
  }
  return 0;
};