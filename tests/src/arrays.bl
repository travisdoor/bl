#load "libc.bl"

main : fn () s32 {
  arr s32[10];

  i := 0;
  while i < 10 {
    arr[i] = i;
    i = i + 1;
  }

  i = 0;
  while i < 10 {
    printf("arr[i] = %d\n", arr[i]);
    i = i + 1;
  }

  // arr.count -> pointing to constant 10
  arr.count;
  
  return 0;
};

count : fn () {};