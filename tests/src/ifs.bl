#load "libc.bl"

main : fn () s32 {
  a := true;
  if (a) {
    puts("ok");
    if (!a) {
      abort();
    } else {
      puts("ok");
    }
  } else {
    abort();
  }

  test();
  return 0;
};

Foo : struct {
  b bool
};

test : fn () {
  foo Foo;
  foo.b = false;
  if (!foo.b) { return; }
  puts("should skip in method!!!");
  abort();
};