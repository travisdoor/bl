#load "libc.bl"

main : fn () -> s32 {
  data s32;
  ptr *s32 := &data;
  ptr = &data;

  if ptr == null {
    puts("ptr is null\n");
  } else {
    puts("ptr is not null\n");
  }

  *ptr = 666;

  printf("*ptr = %d\n", *ptr); // implement should load in ir
  printf("data = %d\n", data);
  return 0;
};