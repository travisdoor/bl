/*
 * Unit test
 * -----------
 * Using test 
 */

fn foo() {}

fn main() i32 {
  using A;
  using A::B; // B must be public
  using A::C; // B must be public

  foo();
  bar();

  var a Foo;

  return 0;
}

module A {
  public fn foo() {}

  public struct Foo {
  }

  public module B {
    public fn bar() {}
    public fn foo() {}

    public struct Foo {
    }
  }

  public module C {
    public fn foo() {}
  }
}
