/*
 * Unit test
 * -----------
 * Using test 
 */

module A {
  public struct Struct {
    i i32
  }

  public enum Enum i32 {
    Foo,
    Bar
  }

  public fn foo() {}

  public module B {
    public fn bar() {}
  }
}

/*
using A;
using A::B; // B must be public
*/

fn main() i32 {
  using A;
  using A::B; // B must be public

  foo();
  bar();

  var s Struct;
  var e Enum;
  e = Enum::Foo;

  A::foo();
  A::B::bar();

  return 0;
}
