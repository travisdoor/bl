/*
 * Unit test
 * -----------
 * While loop.
 */

fn main() i32 {
  var arr i32[256];  
  var i i32;

  while (i < 256) {
    arr[i] = i;
    i = i + 1;
  }


  i = 0;
  while (i < 256) {
    if (arr[i] != i)
      return 1;
    i = i + 1;
  }

  return 0;
}