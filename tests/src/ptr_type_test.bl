/*
 * Unit test
 * -----------
 * Pointer type 
 */

#load "utest.bl"

fn main() i32 {
  var i32_ptr *i32;
  var a i32 = 10;

  i32_ptr = &a; 
  (*i32_ptr) = 1;
  a = (*i32_ptr);
  utest::assert((*i32_ptr) == a);

  return 0;
}

