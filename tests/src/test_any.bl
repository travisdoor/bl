#test "simple function" {
  func : fn (in Any) {
    if (in.type == TypeKind.SInt) {
      // signed integer value
      val := cast(*s32) in.value;
      assert(*val == 666);
    }
  };

  a := 666;

  // this must be generated implicitly by compiler
  tmp Any;
  tmp.value = cast(*u8) (&a);
  tmp.type = typeof(a);

  func(tmp);
};