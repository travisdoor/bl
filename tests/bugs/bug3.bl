RendererVulkan :: struct {
    validation_layers: [1]string
};

renderer_vulkan_new :: fn () *Renderer {
    r := cast(*RendererVulkan) mem_calloc(sizeof(RendererVulkan));
    defer mem_free(cast(*u8) r);

    // this cause error
    r.validation_layers = {:[1]string: "MoltenVK"};
};