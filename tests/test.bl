/* playground */

#load "std/print.bl"

Foo :: struct {
    i: s32,
    j: bool,
    k: [2]s32
};

Enum :: enum {
    A, B, C
};

main :: fn () s32 {
    i := 10;
    text := "hello";

    print("i = %\n", i);

    b := true;
    e := Enum.A;

    foo : Foo;
    foo.i = 666;
    foo.j = true;
    foo.k[0] = 0;
    foo.k[1] = 1;

    k: [2]s32;
    k[0] = 0;
    k[1] = 1;

    sl := {:[]s32: k.len, k.ptr };

    print("hello world\n", 10);
    return 0;
};

