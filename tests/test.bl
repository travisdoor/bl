/* testing playground */

extern fn printf(s string, i i32) i32;

fn main() {
  var i i32;
  //printf("i = %d\n", i);

  var j i32;
  //printf("j = %d\n", j);

  var a A = {
    .a = 0,
    .b = 0
  };

  var b A;
}

struct A {
  a i32,
  b i32
}
