/*
 * testing playground.
*/

fn main() i32 {
  var hello i32; // invalid use of public modificator here

  var b i32 = 20;
  const c i32 = 100; 

  var data my_module::data_t;
  var state my_module::enum_t;

  return 0;
}

module my_module {
  public struct data_t {
    a i32, 
    b i32, 
    c i32
  }

  public enum enum_t {
    A,
    B,
    C
  }
}