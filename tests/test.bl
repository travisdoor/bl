#test "this is my amazing test" {
  a := 0;
  assert(a == 0);
};

main : fn () s32 {
  // unit test can be nested into other functions
  #test "this is my amazing nested test" {
    a := 0;
    assert(a != 0);
  };

  return 0;
};