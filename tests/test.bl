/* playground */

#load "std/print.bl"
#load "std/memory.bl"

main :: fn () s32 {
    i := fib(36);
    print("reusult = %\n", i);
    return 0;
};

fib :: fn (n: s32) s32 {
    if n == 0 || n == 1 {
        return n;
    } else {
        return fib(n-1) + fib(n-2);
    }
};



