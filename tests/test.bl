/* playground */

s8    : #compiler;
s16   : #compiler;
s32   : #compiler;
s64   : #compiler;
u8    : #compiler;
u16   : #compiler;
u32   : #compiler;
u64   : #compiler;
usize : #compiler;

// lax dependency on 'number'
main : fn () {
  bar : 20;
  foo := bar;
  foo = 10 + 2 * bar; 
  //add(10, 20);
};

Int : s32;

add : fn (f s32, s s32) {
  return f + s;
};

/*

ir generation stack:
- by default insert 'main' and all unit tests

*/