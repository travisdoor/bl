// playground 

#import "fs"
#load "std/debug_allocator.bl"

main :: fn () s32 {
    dbgalloc_init();
    defer dbgalloc_terminate();
    
    err :: fs_dir_remove_all("foo");
    if !is_ok(err) { print("%\n", err); }
    else { print("DONE!!!\n"); }
    return 0;
}
