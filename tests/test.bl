/* testing playground */

printf fn (string, size_t) s32 : extern;

main : fn () s32 {
  printf("hello world\n", cast(size_t) 0);

  i s64 := 0;
  i = 1 + 2 * 15 / 3;

  b s64 := 0;
  c s8 := 500;

  if true {} else {}

  loop {
    break;
    continue;
  }

  while true {
    break;
    continue;
  }

  return 0;
};

/*
Add : 0;
Sub : 1;

lambdas_test : fn () {
  i := get(Add);
  i(10, 20);

  i = get(Sub);
  i(10, 20);
};

get : fn (type s32) fn (s32, s32) s32 {
  if type == Add { return fn (a s32, b s32) { return a + b; }; }
  if type == Sub { return fn (a s32, b s32) { return a - b; }; }
};
*/