/*
 * testing playground::
 */

/*
// IDEA: loading of references
#load "std/*"
#load "std/debug.bl"
*/

module m {
  public struct data_t {
    public a i32,
    b i32,
    in inner_t // should work without m
  }

  struct inner_t {
    a i32,
    adam adam_t 
  }

  struct adam_t {
    karel karel_t
  }

  struct karel_t {
    size i32
  }
}

fn main() i32 {
  // struct test
  var data m::data_t; 
  data.b = 10;

  std::println("hello world");
  std::print_1("data.b = %d\n", data.b);

  return 0;
}
