#load "sdl2.bl"

Game : struct {
  window   *SDL_Window,
  will_quit bool,
};

Foo : struct {
  foo *Foo
};

main : fn () s32 {
  foo Foo;
  game Game;
  init(&game); 
  return 0;
};

init : fn (game *Game) {
};

gimme : fn (foo *Foo) {
};

