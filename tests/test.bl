/* playground */

#link "deleteme"

Foo :: struct {
    i: s64,
    j: s64,
    k: s64,
};

foo :: fn (foo: Foo) #extern;

main :: fn () s32 {
    data := {:Foo: 10, 40, 30};
    foo(data);
    return 0;
};
