// playground 
#import "fs"

main :: fn () s32 {
    file, err :: fs_file_open("root");
    if is_ok(err) {
        print("OK!\n");
    } else {
        print("%\n", err);
    }
    fs_file_close(file);
    return 0;
}

