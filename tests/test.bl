/*
 * testing playground.
*/

module c {
  public extern fn puts(s string) i32;
}

module m {
  fn main() i32 {
    var state m.enum_e = m.enum_e.B; 
    var state2 m.enum_e; // should be set to first enum variant if there is one
    var name m.names.Tereza;

    if (state == m.enum_e.A)
       c.puts("state is A");

    return 0;
  }
}

module m {
  public enum enum_e i32 {
    A = 10,
    B = 20,
    //	C  // should be 21
  }

  enum names string { 
    Tereza = "tereza",
    Milan = "milan"
  }

  public struct data_t {
    a i32,
    b string,
    c f64,
    next data_t
  }
}
