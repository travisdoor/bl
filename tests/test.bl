#import "std/table2"

main :: fn () s32 {
    using std;

    slice: []s32;
    slice_init(&slice, 10);
    print("INPUT: %\n", slice);
    loop i := 0; i < slice.len; i += 1 {
        slice[i] = i;
    }

    str := str_new("Hello!");
    print("INPUT: %\n", str);

    path := "/home/travis/develop/foo";
    print("INPUT: %\n", path);

    arr: [..]string_view;
    array_push(&arr, "Hello");
    array_push(&arr, "World");

    using std_experimental;

    tbl: Table(s32, string_view);
    tbl_insert(&tbl, 10, "Hello");
    tbl_insert(&tbl, 11, "Foo");
    return 0;
}
