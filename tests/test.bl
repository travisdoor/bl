/* testing playground */

main : fn () s32 {
  f Foo;
  //test(&f);
  i := 10;
  test2(i);
  return 0;
};

Foo : struct {
  i s32
};

test : fn (foo *Foo) {
};

test2 : fn (i s32) {
};