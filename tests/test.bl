/*
 * This is Biscuit testing script.
 */

extern i32 printf(string s, i32 i);
extern i32 puts(string s);

i32 main() {
  i32 a = 2 + 2 * 12 + 4; // 30
  i32 b = a + get(); // 50
  i32 c;

  // log result
  if (c == 0 && b > 0) {
    c = 2 * 5 - 1 * add(a + 10, b);
    printf("a = %d\n", a);
    printf("b = %d\n", b);
    printf("c = %d\n", c);
  } else {
    puts("error");
  }

  return 0;
}

// return 20
i32 get() {
  i32 ret = 10 * 2;
  return ret;
}

i32 add(i32 a, i32 b) {
  return a + b;
}


