/* testing playground */

extern fn printf(s string, e i32);

enum enum_t i32 {
  Foo,
  Bar
}

fn main() {
  using enum_t;
  var e enum_t;
  e = enum_t::Foo;
  e = Foo;

  printf("Foo = %d\n", Foo);
  printf("Bar = %d\n", Bar);
}








