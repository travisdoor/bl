main :: fn () s32 {
    flags: string;
    set_flag(&flags, "hello");
    return 0;
}

// - first_char :: fn (str: string #readonly) u8 {}
// - #using Enum + use UPPERCASE for variants.
// - #switch ?
// - intel/amd/arm intrinsics???
// - use flat hash table for small scopes for faster lookup? (structs, functions, ...)