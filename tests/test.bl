/* playground */

#load "std/core.bl"

Foo :: enum {
    A :: 10 + 1,
    B :: 20,
    C :: 40,
    D,
    E :: 20,
    F
};

main :: fn () s32 {
    // TODO: Cannot be found!
    Test :: enum {
        A, B
    };
    
    a : Foo = Foo.B;

    if a == Foo.A {
        print("A\n");
    } else {
        print("!A\n");
    }

    return 0;
};

