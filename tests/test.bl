/* playground */

#load "std/core.bl"

main :: fn () s32 {
  str := "I'm glad to see this message!!!\n";
  //str_slice := str_to_slice(str.ptr, str.len);
  str_slice : []u8;
  str_slice.ptr = str.ptr;
  str_slice.len = str.len;
  
  print_str(str_slice);
  print_str(str_slice);
  //print_str(str_slice);
  //print_str(str_slice);

  loop i := 0; i < 1000000; i += 1 {
    foo := i;
  }

  return 0;
};

str_to_slice :: fn (str: *u8, len: usize) []u8 {
  tmp : []u8;
  tmp.ptr = str;
  tmp.len = len;
  return tmp;
};
