/* playground */

//#load "std/core.bl"
//#load "std/debug.bl"

Foo :: struct {
    bar: Bar
};

Bar :: struct {
    f: *fn ()
};

main :: fn () s32 {
    foo: Foo;
    bar := cast(*Bar) &foo;
    
    bar.f = &homo;

    v := homo();
    return 0;
};

homo :: fn () {
    //print("hello!!!\n");
};
