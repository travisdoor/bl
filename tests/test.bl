/* testing playground */

struct foo_t {
  bar bar_t, 
  a i32 = 666,
  b i32 = 10,
  c i32
}

struct bar_t {
  a i32 = 1000
}

extern fn printf(s string, i i32);

fn main() {
  var foo foo_t; 
  // foo.a = 666
  // foo.b = 10
  // foo.c = 0
  // foo.bar.a = 1000

  var i i32;
  // i = 0
  
  printf("foo.a = %d\n", foo.a);
  printf("foo.b = %d\n", foo.b);
  printf("foo.c = %d\n", foo.c);
  printf("foo.bar.a = %d\n", foo.bar.a);
}


