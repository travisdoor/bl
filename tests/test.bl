#import "std/fs"

main :: fn () s32 {
    file, err :: fs_open("file.txt");
    defer fs_close(file);
    if !is_ok(err) { print("%\n", err); }

    _, err2 :: fs_write_string(file, "\nHello!");
    print("%\n", err2);
    
    return 0;
}
