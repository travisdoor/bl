/* testing playground */

extern fn printf(s string, e i32);

module A {
  public module B {
    module C {
      using B::C;
      public fn hello() {}
    }

    fn hello() {
      C::hello();
    }
  }
}

fn main() {
  //using A::B::C;
  //A::B::C::hello();
  //hello();
}
