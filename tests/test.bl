/*
 * testing playground.
 */

module std {
  module inner_module {
    fn no_return() {
    }
  }

  enum state_e {
  }

  fn add(first i32, second i32) i32 {
  }

  fn get_context() context_t {
    var hello i32;
    var a i32 = hello + (10 + add(15 + 2, 256)) + (10);
    10 + 10;
    {
      var test i64;
      {
        var test string;
      }
    }

    std::add();
    std::state_e::A;

    {
      {
        var data i32;
      }
    }
  }
}

struct context_t {
}



