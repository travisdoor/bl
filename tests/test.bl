/* playground */

#load "std/core.bl"

main :: fn () s32 {
    f_ptr := &homo;

    loop i := 0; i < 10; i += 1 {
        f_ptr(i);
    }

    return 0;
};

homo :: fn (v: s32) {
    print("v = %\n", v);
};