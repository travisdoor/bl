/* testing playground */

printf fn (string) i32 : extern;
foo : main;

// lax dependency on printf
/*main : fn () i32 {
  printf("hello");
  //bar : foo;
  return 0;
};*/

main i32;

/*
Add : 0;
Sub : 1;

lambdas_test : fn () {
  i := get(Add);
  i(10, 20);

  i = get(Sub);
  i(10, 20);
};

get : fn (type i32) fn (i32, i32) i32 {
  if type == Add { return fn (a i32, b i32) { return a + b; }; }
  if type == Sub { return fn (a i32, b i32) { return a - b; }; }
  return null;
};
*/

