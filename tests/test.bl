/* testing playground */

printf fn (string) i32 : extern;
puts fn (string) i32 : extern;

main : fn () i32 {
  printf("hello");
  i := 23;
  o := add(i, 12);
  say_hello("hello");

  a : global; // waiting global

  lambda := fn (i i32) {
    i + 10;
  };

  lambda(); // wrong
  global = 10; // wrong
  12 = 2; // wrong

  lambda = fn (i i32, u i32) {
    i - 10;
  };

  return 0;
};

global : 10;

add : fn (a i32, b i32) i32 {
  return a + b;
};