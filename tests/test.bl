/*
 * testing playground.
*/

module m {
  fn main() i32 {
    var state m::enum_e = m::enum_e::A; 
    return 0;
  }
}

module m {
  public enum enum_e i32 { // every enum variant must be bublic by default
    A = 10,
    B = 20,
    C = 30
  }

  public struct data_t {
    a i32,
    b string,
    c f64,
    next data_t
  }
}
