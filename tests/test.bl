/* playground */

#load "std/core.bl"

main :: fn () s32 {
  print("Hello world.\n");

  f : f32 = 0.1f;
  d : f64 = 0.2; 

  assert(f == 0.1f);
  assert(d == 0.2);

  f = cast(f32) d;
  assert(f == 0.2f);
  d = cast(f64) 0.5f;
  assert(d == 0.5);

  return 0;
};
