/*
 * testing playground::
*/

/*
// IDEA: loading of references
#load "std/*"
#load "std/debug.bl"
*/

fn main() i32 {
  // struct test
  var data m::data_t; 
  data.b = 10;
  data.in.a = 666;
  data.in.adam.karel.size = 666;

  std::println("hello world");

  return 0;
}

module m {
  public struct data_t {
    public a i32,
    b string,
    c f64,
    in inner_t // should work without m
  }

  struct inner_t {
    a i32,
    adam adam_t 
  }

  struct adam_t {
    karel karel_t
  }

  struct karel_t {
    size i32
  }
}