/* testing playground */

//extern fn puts(s string) i32;
//extern fn printf(s string, i size_t) i32;

fn main() i32 {
  mut a foo;  
  a.b = false;

  mut c = !a.b;
  
  return 0;
}

struct foo {
  b bool
}
