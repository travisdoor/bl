// this is testing script

extern i32 pri32f(string format, i32 i);
extern i32 puts(string s);

i32 main() {
  i32 a = 0;

  if (a == 1) {
    puts("a is 0");
  } else {
    puts("a is not 0");
  }

  return 0;
}

void assert(bool expr, string msg) {
  if (expr == false) {
    puts(msg);
  }
}
