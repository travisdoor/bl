// this is testing script

extern int puts(string s);
extern int printf(string s, int i);
extern void abort();

int main() {
  int a = 0;

  // problem with bool -> int conversion
  assert(a, "test assert");

  if (a == 0) {
  }

  return 0;
}

int get_one() {
  return 1;
}

void assert(bool expr, string msg) {
  if (expr == true) {
    puts(msg);
  }
}

