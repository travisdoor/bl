/* testing playground */

extern fn puts(s string) i32;
extern fn printf(s string, i size_t) i32;

fn main() i32 {
  mut arr i32 [10];
  take_array(arr);
  return 0;
}

/*fn take_array(arr i32 []) {
  printf("passed arr.count = %d\n", arr.count);
}*/

struct _1 {
  data  *i32,
  count size_t
}
