// Release temporary allocator in __start by default.
// Add function name into CodeLocation structure.

Foo :: struct { a: [12]u32; }

main :: fn () s32 {
    err :: foo();
    a: Foo;
    b: Foo;
    a = b;
    if err { print_err(err); }
    return 0;
}

foo :: fn () Error {
    assert(false);
    return error("This is error!");
}
