/* testing playground */

extern fn puts(s string) i32;

#test
fn test1() {
  target(true);
}

#test
fn test2() {
  target(true);
}

fn target(is_test bool) {
  if is_test {
    puts("testing target"); 
  } else {
    putss("running target"); 
  }
}

fn main() i32 {
  target(false);
  return 0;
}

