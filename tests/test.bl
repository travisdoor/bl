/*
 * testing playground.
 */

/*
fn main(a i32, b i32) i32 {
  var result i32 = add(32, i + 256);
  var bl bool = true;
}

fn add(a i32, b i32) i32 {
}

fn no_return(i f64) {
}
*/

/*
fn no_return(i f64) {
  std::add(123, 321);
}
*/

module std {
  module inner_module {
    fn no_return() {
    }
  }

  enum state_e {
  }

  fn add(first i32, second i32) i32 {
  }

  fn get_context() context_t {
    var hello i32;
    var a i32 = hello + 10 + add(15 + 2, 256);
    10 + 10;
    {
      var test i64;
      {
        var test string;
      }
    }
  }
}

struct context_t {
}



