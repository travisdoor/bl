/* testing playground */

extern fn puts(s string) i32;
extern fn printf(s string, i i32) i32;

fn main() i32 {
  mut foo foo_t;
  // should generate call not copy of expression
  printf("foo.data.count = %d\n", foo.data.count);
  
  return 0;
}

struct foo_t {
  data i32 [c]
}

const c i32 = 10;
