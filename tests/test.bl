/* testing playground */

extern fn printf(s string, i i32) i32;
extern fn puts(s string) i32;

fn get_size() size_t {
  return 10;
}

fn main() i32 {
  printf("A = %d\n", E::A);
  printf("B = %d\n", E::B);
  printf("C = %d\n", E::C);
  printf("D = %d\n", E::D);

  return 0;
}

enum E {
  A = 2, B, C = 10, D = 10
}

