/* testing playground */

extern fn puts(s string) i32;

#test
fn test1() {
  shared(false);  
}

#test
fn test2() {
  shared(false);  
}

fn shared(runtime bool) {
  if runtime {
    puts("run-time");
  } else {
    puts("compile-time");
  }
}

fn main() i32 {
  shared(true);  
  return 0;
}

