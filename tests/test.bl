/* testing playground */

//extern fn puts(s string) i32;
//extern fn printf(s string, i size_t) i32;

fn main() i32 {
  mut arr i32 [10];

  return 0;
}

struct _arr_i32 {
  elems *i32,
  count size_t
}
