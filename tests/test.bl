/* playground */

main :: fn () s32 {
    /*
    arr := {:[2]s32: 1, 2 };
    i := 1;
    foo(arr[i]);
    */

    i := 10;
    t := true;
    f := false;
    ptr := &i;

    print("hello world\n");
    print("i = %\nt = %\nf = %\nptr = %\n", i, t, f, ptr);

    foo(i);
    //bar(i);

    return 0;
};

bar :: fn (v: ...s32) {
};

foo :: fn (v: Any) {
    if v.type_info.kind == TypeKind.Int {
        log("INT");
    }
};

log :: fn (s: string) {
    puts :: fn (v: *u8) #extern;

    puts(s.ptr);
};






