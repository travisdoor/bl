/*
 * This is Biscuit testing script.
 */

extern i32 printf(string s, i32 i);

i32 main() {
  // should be 28 but result is 34 
  // i32 a;
  2 + 4 * 6 + 2;
  // on output stack + * +
  // printf("a = %d\n", a);
  return 0;
}

