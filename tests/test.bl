/*
 * testing playground::
*/

/*
// IDEA: loading of references
#load "std/*"
#load "std/debug.bl"
*/

fn main() i32 {
  // struct test
  var data m::data_t; 
  //data::a = 666;
  //data.in.a = 666;

  std::println("hello world");

  return 0;
}

module m {
  public struct data_t {
    public a i32,
    b string,
    c f64,
    in inner_t // should work without m
  }

  struct inner_t {
    a i32
  }
}