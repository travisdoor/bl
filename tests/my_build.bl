/* build setup */

build :: fn () #build_entry {
    exe := add_executable("myprogram");
    add_unit(exe, "test.bl");
    link_library(exe, "glfw");
    link_library(exe, "glfw");
    link_library(exe, "glfw");
    link_library(exe, "glfw");
}


/*

Assembly opt:
- Debug/Release
- Opt level
- output dir

 */

